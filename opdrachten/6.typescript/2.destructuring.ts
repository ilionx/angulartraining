// 1. Gebruik destructuring om in één regel code de drie waarden 'a', 'b' en 'c' uit arrayOfArrays toe te wijzen aan drie variabelen a, b
// en c.

const arrayOfArrays = [['a', 'b'], 'c'];

// 2. Gebruik de spread operator om een exacte kopie van het onderstaande object te maken.

const person = {
  name: 'Willem Alexander',
  profession: 'King',
  partner: 'Maxima'
};

// 3. Hetzelfde als 2, maar vervang nu de waarde van partner door 'Maxima Zorreguieta'.

// 4. Gebruik de spread operator om alle eigenschappen van person behalve 'profession' toe te wijzen aan een variabele 'rest'.
